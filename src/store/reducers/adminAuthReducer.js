const initialState = {
  admin: null,
};

const adminAuthReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default adminAuthReducer;
